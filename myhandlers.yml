---
- hosts: mywebservers
  become: yes
  tasks:
  - include_tasks: myfile.yml
  
  - name: i want to install webserver
    apt:
     name: apache2
     state: present


  - name: i want to restart my webserver
    service:
      name: apache2
      state: restarted
    